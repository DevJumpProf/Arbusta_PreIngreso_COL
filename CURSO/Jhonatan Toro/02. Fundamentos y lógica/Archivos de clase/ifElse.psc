
Algoritmo ifElse
	
	// if /else --> si se cumple la condición (es verdadera), ejecutará un bloque de código.

	Definir numeroIngresado Como Entero
	
	Escribir 'Ingrese un numero del 1 al 10: '
	Leer numeroIngresado
	Si (numeroIngresado<=10) && (numeroIngresado>=1) Entonces
		Escribir 'Perfecto'
	SiNo
		Escribir 'Incorrecto'
	FinSi
	
FinAlgoritmo